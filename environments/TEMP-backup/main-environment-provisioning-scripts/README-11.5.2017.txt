Temporarily removing these terraform provisioning scripts from the main terraform scripts.

Doing this so I can play around with manually provisioning the servers.  Can destroy and re-screate the environment without provisioning each time.

These scripts MUST go back into the main terrorform scripts dir once this experimentation is done.